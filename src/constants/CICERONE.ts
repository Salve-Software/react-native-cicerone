/** Geometry taken from the Rotuz clickable prototype. */
export const CICERONE = {
  stepPadding: 8,
  minRingRadius: 16,
  minHoleRadius: 15,
  cardOffset: 18,
  cardWidth: 284,
  cardScreenMargin: 16,
  arrowSize: 14,
  arrowOverlap: 6,
} as const;

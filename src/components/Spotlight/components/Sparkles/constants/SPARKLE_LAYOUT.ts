import type { ISparklePosition } from '@/components/Spotlight/components/Sparkles/types';

/** Offsets are relative to the ring box, as in the prototype. */
export const SPARKLE_LAYOUT: ISparklePosition[] = [
  { left: -8, top: -10 },
  { right: 24, top: -14 },
  { right: -9, bottom: -11 },
  { left: 32, bottom: -15 },
];
